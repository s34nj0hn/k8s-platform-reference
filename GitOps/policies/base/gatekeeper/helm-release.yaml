apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gatekeeper
  namespace: gatekeeper-system
spec:
  interval: 30m
  chart:
    spec:
      chart: gatekeeper
      version: 3.17.1
      sourceRef:
        kind: HelmRepository
        name: gatekeeper
        namespace: gatekeeper-system
      interval: 1h
